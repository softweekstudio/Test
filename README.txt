FFmpeg Standalone Multi-Thread Test

Upload BOTH files to the root of your GitHub Pages repository:
- index.html
- sw.js

Open the GitHub Pages HTTPS address, not the downloaded HTML file.
Reload once after the service worker installs.

The page must show: Multi-thread: ON
Then test first with the 15-second video.
Do not use the 3-hour video until the 15-second test finishes correctly.
